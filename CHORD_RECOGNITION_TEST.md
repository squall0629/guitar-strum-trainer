# 和弦识别功能测试说明

## 功能概述

已实现的和弦识别功能包含以下核心模块：

### 技术栈
- **音频分析：** Web Audio API + FFT（自己实现）
- **和弦识别：** tonaljs（音符→和弦识别）
- **和弦字典 + 指法图：** chordictionaryjs（和弦→SVG 指法图）

### 文件结构
```
guitar-strum-trainer/
├── chord-library.js      # 和弦库模块（集成 chordictionary）
├── chord-detector.js     # 和弦检测器（集成 tonaljs + chordictionary）
├── renderer.js           # 主渲染逻辑（已集成和弦识别）
├── index.html            # UI（已添加和弦训练界面）
└── chordictionary.min.css # 和弦指法图样式
```

## 测试步骤

### 1. 启动应用
```bash
cd /home/squall0629/.openclaw/workspace-se/projects/guitar-strum-trainer
npm start
```

浏览器打开：http://localhost:3000

### 2. 基础和弦识别测试

#### 测试用例 TC-001：单和弦识别
1. 点击"开始练习"按钮
2. 弹奏 C 和弦（扫弦）
3. 观察"识别"显示区域

**预期结果：**
- 识别显示：C
- 置信度：≥65%
- 显示 C 和弦的 SVG 指法图

#### 测试用例 TC-002：和弦转换识别
1. 选择和弦进行：C→G→Am→F
2. 依次弹奏每个和弦
3. 观察识别结果和转换时间

**预期结果：**
- 每个和弦识别准确率 ≥75%
- 转换时间正确记录
- UI 显示当前和弦和下一个和弦

### 3. 训练模式测试

#### 测试用例 TC-003：预设进行训练
1. 选择"预设进行"模式
2. 选择"流行 1645 (C→Am→F→G)"
3. 跟随节拍器弹奏

**预期结果：**
- 系统提示当前和弦
- 识别正确时显示"✓ 和弦正确！"
- 识别错误时显示"⚠ 应该是 X，检测到 Y"

#### 测试用例 TC-004：自定义进行
1. 选择"自定义"模式
2. 点击和弦按钮：C → G → D → Am
3. 点击"保存进行"，命名为"我的进行"
4. 开始练习

**预期结果：**
- 自定义和弦进行正确显示
- 可以正常识别和弦转换

#### 测试用例 TC-005：自由练习模式
1. 选择"自由练习"模式
2. 随意弹奏任意和弦

**预期结果：**
- 系统自动识别所有弹奏的和弦
- 不显示"错误"提示
- 记录练习的和弦序列

### 4. 性能测试

#### 测试用例 TC-006：识别延迟
1. 开始练习
2. 弹奏一个和弦
3. 使用秒表测量从弹奏到显示的时间

**预期结果：**
- 识别延迟 < 100ms

#### 测试用例 TC-007：CPU/内存占用
1. 打开浏览器开发者工具
2. 查看 Performance 面板
3. 持续弹奏 1 分钟

**预期结果：**
- CPU 占用 < 15%
- 内存占用 < 200MB

### 5. 和弦库覆盖测试

#### 测试用例 TC-008：10 个基础和弦识别
依次测试以下和弦：
- C, G, D, Am, Em, E, A, F, Dm, Cmaj7

**预期结果：**
- 每个和弦都能被识别
- 指法图正确显示
- 开放和弦（C/G/D/Am/Em）识别率 ≥85%

## 已知限制

### 当前版本限制
1. **和弦类型：** 仅支持开放和弦（无大横按）
2. **吉他类型：** 针对民谣吉他优化（钢弦）
3. **音频输入：** 需要清晰的单音扫弦，避免杂音

### 识别率影响因素
1. **吉他音准：** 吉他需要调音准确
2. **麦克风质量：** 外置麦克风优于内置
3. **环境噪音：** 安静环境识别率更高
4. **扫弦技巧：** 清晰的扫弦优于分解和弦

## 调试工具

### 浏览器控制台日志
打开开发者工具（F12），查看以下日志：
- `[ChordTraining]` - 和弦训练相关
- `[ChordDetector]` - 和弦检测相关
- `[ChordLibrary]` - 和弦库相关

### 识别数据查看
在控制台中输入：
```javascript
// 查看上次识别结果
lastRecognizedChord

// 查看检测器统计
transitionDetector.getStats()

// 查看当前和弦进行
currentProgression
```

## 常见问题

### Q1: 识别率很低怎么办？
**A:** 调整麦克风灵敏度：
1. 拖动"麦克风灵敏度"滑块
2. 调高灵敏度（向右）更容易触发
3. 调低灵敏度（向左）减少误触发

### Q2: 和弦指法图不显示？
**A:** 检查：
1. chordictionary.min.css 是否正确加载
2. 浏览器控制台是否有 SVG 相关错误
3. Canvas 元素是否正常渲染

### Q3: 转换时间显示为"--"？
**A:** 需要完成至少一次和弦转换才会显示

## 验收标准

根据 PRD 文档，首期功能验收标准：

| 指标 | 目标值 | 测试方法 |
|------|--------|----------|
| 和弦识别准确率 | ≥85%（开放和弦） | TC-008 |
| 识别延迟 | < 100ms | TC-006 |
| 支持和弦数量 | ≥10 个 | TC-008 |
| CPU 占用 | < 15% | TC-007 |
| 内存占用 | < 200MB | TC-007 |

## 下一步迭代

### Phase 2（后续开发）
- [ ] 机器学习和弦识别（CNN 模型）
- [ ] 支持大横按和弦（F、Bm 等）
- [ ] 支持七和弦、九和弦等复杂和弦
- [ ] 智能调音器集成

### Phase 3（数据可视化）
- [ ] 练习报告弹窗
- [ ] 准确率/转换时间趋势图
- [ ] 历史记录增强

---

**文档版本：** v1.0  
**创建日期：** 2026-04-03  
**技术负责人：** 老马
