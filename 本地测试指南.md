# 吉他扫弦练习助手 - 本地测试指南

## 🚀 快速启动（推荐）

### 方法 1：使用 npm start（最简单）

```bash
cd /Users/yang/.openclaw/workspace-se/projects/guitar-strum-trainer
npm start
```

**访问地址：** http://localhost:3000

---

### 方法 2：使用 Python HTTP 服务器

```bash
cd /Users/yang/.openclaw/workspace-se/projects/guitar-strum-trainer
python3 -m http.server 8080
```

**访问地址：** http://localhost:8080

---

## ⚠️ 重要：不要用 file:// 协议打开！

**错误方式：** 直接双击 `index.html` 或用 `file:///...` 打开
- ❌ 浏览器会禁止麦克风访问
- ❌ 会报错 `getUserMedia not supported`

**正确方式：** 必须通过 HTTP 服务器访问
- ✅ `http://localhost:3000`
- ✅ `http://localhost:8080`

---

## 🎤 麦克风权限设置

### Chrome/Edge 浏览器

1. 访问 `http://localhost:3000`
2. 点击"开始练习"按钮
3. 浏览器会弹出权限请求 → 点击 **"允许"**
4. 如果之前点了"禁止"：
   - 点击地址栏左侧的 🔒 锁图标
   - 找到"麦克风" → 改为"允许"
   - 刷新页面

### Firefox 浏览器

1. 访问 `http://localhost:3000`
2. 点击"开始练习"按钮
3. 浏览器会弹出权限请求 → 点击 **"允许"**
4. 如果之前点了"禁止"：
   - 点击地址栏左侧的图标
   - 清除权限设置
   - 刷新页面重新授权

---

## 📋 测试流程

### 1. 启动本地服务器

```bash
cd /Users/yang/.openclaw/workspace-se/projects/guitar-strum-trainer
npm start
```

### 2. 打开浏览器

访问：http://localhost:3000

### 3. 打开开发者工具

按 `F12` 或右键 → 检查 → Console

### 4. 开始测试

1. 点击"🎤 开始练习"
2. 允许麦克风权限
3. 扫弦（弹吉他）
4. 观察控制台日志和评分变化

### 5. 查看关键日志

搜索 `[DEBUG]` 标记：
- `[DEBUG 麦克风诊断]` - 麦克风输入信号
- `[DEBUG detectStrum]` - 扫弦检测
- `[DEBUG updateScores]` - 评分计算

### 6. 反馈问题

**如果发现问题：**
1. 截图控制台日志
2. 截图评分面板
3. 描述具体现象（如：扫弦后分数不变）
4. 发给老马

---

## 🛠️ 常见问题

### Q1: 点击"开始练习"没反应

**检查：**
- 是否通过 HTTP 服务器访问？（不能用 file://）
- 控制台有没有红色错误？

### Q2: 浏览器提示"麦克风权限被拒绝"

**解决：**
1. 点击地址栏左侧的 🔒 图标
2. 找到"麦克风"选项
3. 改为"允许"
4. 刷新页面

### Q3: 波形图有显示，但评分不变

**可能原因：**
- 扫弦检测阈值太高
- 麦克风输入信号太弱

**检查：**
- 控制台 `[DEBUG 麦克风诊断]` 的 `rms` 值
- 正常扫弦应该 `rms > 0.05`

### Q4: 评分显示 NaN

**已修复！** 如果还有问题，检查控制台错误日志。

---

## 📊 测试反馈模板

```
【测试时间】2026-04-05 13:30
【测试场景】轻柔扫弦 / 正常扫弦 / 快速扫弦
【测试结果】
- 波形图：✅ 有显示 / ❌ 无显示
- 评分系统：✅ 正常更新 / ❌ 不变 / ❌ 显示 NaN
- 麦克风诊断：rms=0.XXX, totalSpectrumEnergy=XXX
【问题描述】
（如果有问题，详细描述现象）
【控制台日志】
（粘贴关键日志，特别是 [DEBUG] 开头的）
```

---

## 🎯 下一步

1. 本地测试通过
2. 老马推送 GitHub
3. Vercel 自动部署
4. 线上验证

**测试不通过不推送！** ✅
